# Editors

Este diretorio concentra os scripts auxiliares dos editores internos do Brasix.

- `map editor`: usa a interface em `/editor/map` e persiste direto nos JSONs oficiais do `v1`.
- proximos editores podem nascer aqui sem criar uma camada paralela de dados.
